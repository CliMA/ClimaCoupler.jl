agents:
  queue: gcp
  slurm_time: 24:00:00
  # modules: climacommon/2025_05_15

env:
  JULIA_LOAD_PATH: "${JULIA_LOAD_PATH}:${BUILDKITE_BUILD_CHECKOUT_PATH}/.buildkite"
  OPENBLAS_NUM_THREADS: 1
  JULIA_NVTX_CALLBACKS: gc
  OMPI_MCA_opal_warn_on_missing_libcuda: 0
  JULIA_MAX_NUM_PRECOMPILE_FILES: 100
  GKSwstype: 100
  SLURM_KILL_BAD_EXIT: 1
  CONFIG_PATH: "config/longrun_configs"

timeout_in_minutes: 1800 # 30 hours

steps:

  - label: "init :computer:"
    key: "init"
    command: |
      srun -n 1 bash -lc "julia --project=experiments/ClimaEarth -e '
        using CUDA
        A = CUDA.fill(1.0f0, 1024, 1024)
        B = similar(v)
        copy!(B, v)
        synchronize(); println(\"OK\")
      '"
